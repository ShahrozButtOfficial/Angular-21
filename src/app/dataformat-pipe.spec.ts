import { DataformatPipe } from './dataformat-pipe';

describe('DataformatPipe', () => {
  it('create an instance', () => {
    const pipe = new DataformatPipe();
    expect(pipe).toBeTruthy();
  });
});
